{
  "name": "safe-ai-hacker",
  "version": "0.1.0",
  "private": true,
  "description": "SAFE AI Hacker (SAHW) — loop-engineered, deterministic-first, safety-gated autonomous web pentest control plane on the OpenCode SDK.",
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.18.31"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
