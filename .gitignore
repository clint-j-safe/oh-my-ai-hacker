# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
build/
*.tsbuildinfo

# Test output
coverage/

# Workspaces / scratch (superpowers SDD + git worktrees)
.superpowers/
.worktrees/
worktrees/

# Secrets / runtime state
.env
.env.*
!.env.example
spill_store/
secrets/

# OS / editor
.DS_Store
*.log

# Raw scan output — live target credentials/PII; redact before sharing
results/

# Engagement reports & benchmarking data — stay local, never tracked (live findings/PII)
docs/htb/
human-test/

# Scorer, benchmark ground-truth, and design/prompt docs — local only, not needed to OPERATE the framework
bench/
docs/bench/
docs/PROMPTS.md
docs/LOOP.md
docs/research/
