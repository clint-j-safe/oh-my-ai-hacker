// mcp-patt — PayloadsAllTheThings retrieval MCP server (stub).
// Placeholder referenced by opencode.json. Serves curated payload snippets to
// the novelty-synthesizer agent. Full payload corpus wired in the build phase.
process.stdin.resume();
console.error("[mcp-patt] stub loaded — no payload corpus mounted yet");
