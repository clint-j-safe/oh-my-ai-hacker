// mcp-oast — out-of-band callback query MCP server (stub).
// Placeholder referenced by opencode.json. Will query the OOB listener's
// callback log (see src/oob.ts CallbackLog) so the stateful-prober agent can
// correlate canary callbacks to the payload that triggered them.
process.stdin.resume();
console.error("[mcp-oast] stub loaded — callback query wiring pending");
